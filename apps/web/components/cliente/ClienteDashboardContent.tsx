'use client';

import { GrowsHomeStitch } from '@/components/cliente/proyectos-canvas/GrowsHomeStitch';

/** Home cliente = layout tipo Stitch (sidebar proyectos + chat central), azul corporativo. */
export function ClienteDashboardContent() {
  return <GrowsHomeStitch />;
}
